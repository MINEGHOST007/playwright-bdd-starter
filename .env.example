# Base URL for tests (used by Playwright's baseURL config)
BASE_URL=https://playwright.dev

# Test timeout in milliseconds
TIMEOUT=30000

# Number of retries on failure
RETRIES=0

# Number of parallel workers
WORKERS=4
